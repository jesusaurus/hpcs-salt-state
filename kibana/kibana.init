# kibana - web instance
#
# Copied from http://cookbook.logstash.net/recipes/using-upstart/

description     "kibana web instance"

start on virtual-filesystems
stop on runlevel [06]

# Respawn it if the process exits
respawn
respawn limit 5 30
expect fork

chdir /mnt/kibana

script

  su -s /bin/sh -c 'exec "$0" "$@"' kibana -- /usr/bin/ruby kibana-daemon.rb start &
  emit kibana-running
end script

